version: '3.5'

services:
  db_auth:
    container_name: db_auth
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=robots
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=root
    image: postgres:latest